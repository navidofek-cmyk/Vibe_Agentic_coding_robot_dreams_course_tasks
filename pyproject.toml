[project]
name = "hw-01-tool-calling"
version = "0.1.0"
description = "Simple OpenAI tool-calling homework runnable with uv"
requires-python = ">=3.11"
dependencies = [
  "openai>=2.0.0",
]

[tool.uv]
package = false
